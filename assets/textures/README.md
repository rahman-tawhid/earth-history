Place real paleogeographic maps here (equirectangular projection, e.g. 2048x1024).

The current build generates procedural placeholder paleomaps at runtime
(src/textures/procedural.js) so the experience is fully functional with zero
binary assets. To swap in real Scotese-style maps, see README.md § Swapping in
real paleomaps.
